# Architecture

This MVP is a local-first Chrome Manifest V3 extension. The extension scans only the active page after a user action, builds a fill plan, asks the user to review it, then fills accepted fields without submitting the form.

LinkedIn is intentionally copy-assist only and is not included in automated host permissions.
